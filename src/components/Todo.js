import React from 'react'

export default function Todo() {
    return (
        <div>
            <h1>{Todo.title}</h1>
            <button>Delete</button>
            <button>Complete</button>

        </div>
    )
}
